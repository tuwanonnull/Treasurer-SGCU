{
  "name": "treasurer-sgcu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-static.mjs",
    "check": "node tools/build-static.mjs --check && node tools/check-audit-log.mjs",
    "check:audit": "node tools/check-audit-log.mjs",
    "test:push": "npm --prefix functions test",
    "serve:dist": "python3 -m http.server 4173 --directory dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
